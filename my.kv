#:kivy 2.1.0

<MyGrid>:

    
    GridLayout:
        cols:1
        size: root.width-50, root.height-80
        pos: 50, 50

        # 2
        StackLayout:
            orientation: 'lr-tb'
            # Nimi
            Label:
                text: "Nimi"
                size_hint:(.1, .125)
            TextInput:
                text: ''
                id: nimi 
                multiline: False
                hint_text: "Nimi"
                size_hint:(.15, .125)
            
            Label:
                text: "Aasta"
                size_hint:(.1, .167)
            TextInput:
                size_hint:(.15, .167)
                text: ''
                hint_text: "Aasta"
                id: aasta 
                multiline: False
            
            Label:
                size_hint:(.3, .2)
                text: "Kirjeldus"

            Button:
                size_hint:(.2, .25)
                text:"Lisa"
                on_press: app.lisa_event()

            # eKr
            Label:
                size_hint:(.1, .125)
                text: "eKr"
            CheckBox:
                id: ekr
                size_hint:(.15, .125)
            # kuu
            Label:
                text: "Kuu"
                size_hint:(.1, .166)
            TextInput:
                text: '01'
                hint_text: "Kuu"
                id: kuu 
                multiline: False
                size_hint:(.15, .167)

            TextInput:
                text: ''
                id: kirjeldus
                hint_text: "Kirjeldus"
                multiline: True
                size_hint:(.3, .29999)           
            # Värv
            ColorPicker:
                id: varv
                size_hint:(.25, .25)

            Label:
                text: "Päev"
                size_hint:(.1, .167)

            TextInput:
                text: '01'
                hint_text: "Päev"
                id: paev 
                multiline: False
                size_hint:(.15, .167)

            Label:
                size_hint: (.3, .00001)

            Button:
                text: "Kustuta"
                size_hint:(.2, .25)
                on_press: app.kustuta_event()
     
        Label:
            canvas:
                Line: 
                    points: 0, self.center_y, self.right, self.center_y